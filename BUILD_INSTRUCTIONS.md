# BUILD INSTRUCTIONS: listing-profit-dashboard-etsy-makers

## Overview

- **App Name:** listing-profit-dashboard-etsy-makers
- **Repository:** listing-profit-dashboard-etsy-makers
- **Tech Stack:** Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- **Description:** A profitability and quoting app for Etsy sellers that calculates per-listing margin using materials, production time, fees, packaging, shipping, and SKU-level costs. It helps makers understand true profitability, price custom work, and decide which listings to scale or retire.

---

## Tech Stack & Setup

### Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui

```bash
npx create-next-app@latest listing-profit-dashboard-etsy-makers --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
cd listing-profit-dashboard-etsy-makers
# shadcn/ui (always include)
npx shadcn@latest init -d
npx shadcn@latest add button card input label form toast badge
# Only add Prisma if the PRD requires data persistence:
# npm install prisma @prisma/client
# npx prisma init
# Only add NextAuth if the PRD requires user login:
# npm install next-auth@beta @auth/prisma-adapter bcryptjs
# npm install -D @types/bcryptjs
# Only add Stripe if the PRD explicitly requires payments:
# npm install stripe @stripe/stripe-js
# Only add Resend if the PRD explicitly requires email:
# npm install resend
```

### Configuration

Add to next.config.ts:
```ts
const nextConfig = {
  output: "standalone",
};
export default nextConfig;
```

### Development

```bash
npm run dev
```

### Build

```bash
npm run build
```

---

## Product Requirements Document

The full PRD is included below. Implement ALL features described.

---

# Listing Profit Dashboard for Etsy Makers

*Generated by Forge on 2026-05-12T09:19:36.626Z — score 8.2/10*

## Overview

**Listing Profit Dashboard for Etsy Makers** is a **Next.js SaaS web app** for Etsy 3D print, laser-cut, and handmade sellers who need to know the true profit of each listing before they scale it, discount it, or take custom orders. The product solves a recurring pricing problem: Etsy sellers often estimate prices loosely while fees, materials, labor time, packaging, shipping, and SKU-level production costs constantly change. The app provides listing-level margin calculation, reusable cost templates, custom quote generation, and profitability reporting so sellers can price work confidently and stop underpricing products that look successful but are actually low-margin.

## Why this will succeed

This opportunity is credible because there are already working products around adjacent parts of the workflow, and the gap is clear: Etsy sellers need **storefront-specific profitability**, not generic maker costing or broad seller operations software.

### Comparable products that are working

1. **Craftybase** — https://craftybase.com  
   - Size: described in the research as an **established inventory and bookkeeping tool for handmade sellers** and a **long-running brand in the handmade/Etsy operations category**.  
   - What works: Craftybase proves handmade sellers will pay for operational software tied to Etsy businesses, especially when it helps with inventory, accounting, and pricing-related workflows.

2. **EverBee** — https://www.everbee.io  
   - Size: described in the research as a **well-known Etsy seller tool** with **large visibility in the Etsy creator/YouTube ecosystem**.  
   - What works: EverBee validates that Etsy sellers actively buy specialized software, and that there is already a creator-led distribution path in the Etsy tooling category.

3. **FabLog** — https://www.fablog.io  
   - Size: described in the research as a **recognized maker-oriented cost/profit tracking product**.  
   - What works: FabLog proves demand for production costing and profitability tracking among makers, particularly where machine/material costs matter.

### Specific data points from research

- Reachable niche estimate: **3,000–8,000 active Etsy 3D print and laser-cut sellers**, specifically by focusing on sellers active in communities and YouTube maker audiences.
- Pricing fit from research: **strong fit for $12–24/mo**, with the revenue projection expanding that to **$19/mo standard and $39/mo pro**.
- Conservative revenue scenario from research: **100 users at roughly $19–20/mo supports about $2,000 MRR**.
- Larger solo-business scenario from research: **300–400 mixed-plan subscribers** supports **$12,000/month**.
- The opportunity was surfaced by **2 sources: Product Hunt and autocomplete**, which is not deep market research by itself, but it is a real signal that people search for this problem space.
- Keyword cluster from research is high-intent and directly commercial:
  - “etsy profit tracker for handmade sellers”
  - “etsy listing margin calculator”
  - “3d print seller cost calculator”
  - “laser cut etsy pricing tool”
  - “best app for etsy sellers”
- Distribution research identified concrete communities and channels:
  - **r/EtsySellers**
  - **r/Etsy**
  - **r/3Dprinting**
  - **r/lasercutting**
  - maker Discord servers
  - Etsy Facebook groups
  - YouTube audiences around Etsy side hustles, 3D printing shops, and Glowforge/laser businesses

### Why this specific opportunity is different / better

This product should not position as “general bookkeeping” or “generic product cost tracking.” The gap is narrower and more defensible:

- **Against Craftybase:** Craftybase is broader inventory/accounting software. The research explicitly notes it can feel **heavy** for sellers who mainly want **listing-level margin clarity** and **fast quoting**. This product wins by being lighter, faster, and focused on “Should I keep selling this listing at this price?”
- **Against EverBee:** EverBee is known for **product research and analytics**, not true profitability. This product wins by owning the **pricing/costing workflow after a seller has a product idea**.
- **Against FabLog:** FabLog validates maker costing demand, but the research says it is more **general maker/manufacturing costing** and likely misses **Etsy fee structures, shipping realities, listing-based decisions, and printable customer quote/invoice workflows**. This product wins by modeling **Etsy-native economics**.

The differentiation is concrete: **a storefront-specific profitability dashboard for Etsy physical-product sellers, especially 3D print and laser-cut shops, with custom quote support and listing-level decision-making**. That is a different buyer and a different delivery model than generic cost tracking.

## How this will succeed

### Primary channel

**SEO**

This should be SEO-first because the research already identifies a cluster of high-intent long-tail keywords tied directly to pain and purchase intent: “etsy listing margin calculator,” “3d print seller cost calculator,” and “laser cut etsy pricing tool.” These are more actionable than broad social marketing because the user is already trying to solve a pricing problem. The product can ship a free public calculator as the acquisition wedge, then convert serious sellers into paid accounts for saved listings, templates, and reporting.

**Secondary channel:** community seeding in Etsy/maker communities to get initial traffic, testimonials, and keyword-language feedback while SEO pages mature.

### Channel-specific playbook

Because the upstream research did **not provide actual monthly search volume numbers or current SERP breakdowns**, this PRD will not invent them. The keyword plan below is grounded in the research’s named keyword cluster and should be validated with Ahrefs/LowFruits/Google Keyword Planner before writing all pages.

#### Exact keyword cluster

Primary intent keywords from research:
- etsy listing margin calculator
- etsy profit tracker for handmade sellers
- 3d print seller cost calculator
- laser cut etsy pricing tool
- etsy 3d print profit calculator
- how to price custom 3d prints on Etsy
- Etsy fee calculator for handmade sellers
- best app for etsy sellers
- apps for etsy sellers

#### Current SERP competition

Based on the research, the likely competing domains for this problem area are:
- Craftybase
- EverBee
- FabLog
- likely Etsy blog/forum content and generic fee calculators

The research does **not** include a live SERP scrape, so the requirement for launch is to manually inspect the top 10 for each target keyword before finalizing page copy. The realistic wedge is not broad “Etsy app” terms but long-tail pages where incumbents are broader than the specific use case.

#### Can a new site realistically break in?

Yes, but only on **specific long-tail calculator and use-case pages**, not broad head terms. A new domain should not expect to rank quickly for “apps for Etsy sellers,” but can compete for narrower intent such as:
- etsy 3d print profit calculator
- laser cut etsy pricing tool
- how to price custom 3d prints on Etsy
- etsy listing margin calculator

The acquisition strategy depends on shipping pages that are genuinely useful calculators/templates, not thin marketing pages.

#### First 10 pages to build

1. `/` — homepage focused on “Know the real profit of every Etsy listing”
2. `/etsy-listing-margin-calculator` — free public calculator landing page
3. `/etsy-fee-calculator-handmade-sellers` — fee-focused SEO calculator page
4. `/3d-print-seller-cost-calculator` — niche calculator page
5. `/laser-cut-etsy-pricing-tool` — niche pricing page
6. `/how-to-price-custom-3d-prints-on-etsy` — educational article + CTA
7. `/how-to-price-laser-cut-products-on-etsy` — educational article + CTA
8. `/compare/craftybase-vs-listing-profit-dashboard` — comparison page
9. `/compare/fablog-vs-listing-profit-dashboard` — comparison page
10. `/templates/custom-order-quote-template-etsy` — free template lead magnet page

### Week-by-week execution plan

#### Week 4 (beta + warm-up)

By Week 4, while product polish is finishing:

- Publish 4 seed pages:
  - Etsy Listing Margin Calculator
  - Etsy Fee Calculator for Handmade Sellers
  - How to Price Custom 3D Prints on Etsy
  - How to Price Laser Cut Products on Etsy
- Set up:
  - Google Search Console
  - Google Analytics
  - PostHog for event analytics
  - Bing Webmaster Tools
- Build outreach sheet with:
  - 30 Etsy YouTube creators
  - 20 maker/laser/3D print Discord servers
  - 20 Etsy Facebook groups
  - 10 subreddits/threads where pricing questions recur
- Warm up Reddit/community accounts for 2+ weeks before launch:
  - Comment on pricing/fees threads with actual help
  - Post one free spreadsheet/template before mentioning the app
- Create lead magnet:
  - “Etsy Custom Order Pricing Worksheet” as downloadable PDF/CSV
- Recruit 10–20 beta users from communities by offering free 60-day Pro access in exchange for screenshots/testimonials and permission to quote feedback.

#### Week 5 (launch)

##### Day 1 — Monday
- Publish Product Hunt-style maker post on personal LinkedIn/X if available, but this is not the main channel.
- Send beta users onboarding email asking them to import 3 listings and reply with confusion points.
- Publish comparison page: “Craftybase vs a listing-level profit dashboard for Etsy makers.”

##### Day 2 — Tuesday
- Post in **r/EtsySellers**:  
  Title: **I built a free Etsy listing margin calculator for handmade, 3D print, and laser-cut sellers — looking for feedback**  
  Content angle: lead with the free calculator and explain the exact formula categories: fees, materials, labor, packaging, shipping. No hard sell on paid plans.
- Best posting time: morning US Eastern to catch US seller activity.

##### Day 3 — Wednesday
- Post in **r/3Dprinting**:  
  Title: **Free calculator for pricing Etsy 3D prints with material, machine time, fees, packaging, and shipping**  
  Angle: show a real example of filament + machine time + Etsy fees affecting margin.
- Email 10 YouTube creators with a short pitch:
  - “I made a free Etsy 3D print profit calculator your audience can use.”
  - Offer a custom co-branded calculator page or free access.

##### Day 4 — Thursday
- Publish article: “Why your Etsy bestseller may be your least profitable listing.”
- Share in relevant Facebook groups only if promo rules allow; otherwise share the educational article with one screenshot and no direct subscription ask.

##### Day 5 — Friday
- Post in **r/lasercutting**:  
  Title: **I made a pricing calculator for Etsy laser-cut sellers — materials, machine time, shipping, packaging, fees**
- Reach out to 10 additional creators in Glowforge/laser circles.

##### Day 6 — Saturday
- Review analytics:
  - calculator starts
  - signups
  - conversion from calculator to account creation
  - which use-case pages convert best
- Push one UI copy fix and one onboarding improvement based on session recordings.

##### Day 7 — Sunday
- Send “week 1 findings” email to beta list with educational tips and ask users to submit one pricing question.

#### Weeks 6-8 (compound)

Weekly repeats:
- Publish 2 SEO pages/articles per week
- Do 3 community comments/replies per week with value-first answers
- Email 5 creators/partners per week
- Interview 2 users per week
- Add 1 new calculator/template or use-case page every week

One-time improvements:
- Build a public example library:
  - 3D printed planter
  - laser-cut sign
  - custom cake topper
  - wedding favor bundle
- Add testimonial blocks from beta users
- Add “custom quote template” export if users ask for it repeatedly

If SEO underperforms by week 6:
- Double down on community seeding and creator partnerships
- Publish comparison pages and specific use-case pages instead of broad educational posts
- Ship a no-signup calculator embed or downloadable worksheet to improve sharing

#### 3-month targets

- 10 SEO pages live by end of month 2
- 30–50 beta users by end of month 2
- 100+ email captures from free calculator traffic by month 3
- 20–40 paying users by month 3
- At least one top-10 ranking for a narrow long-tail term such as:
  - etsy listing margin calculator
  - laser cut etsy pricing tool
  - 3d print seller cost calculator  
  This is a stretch target, not a guaranteed outcome, because the research did not provide current SERP difficulty data.
- 3 creator mentions, community referrals, or backlinks by month 3

### Budget & CAC

#### 3-month budget
- Tools:
  - Domain + hosting on Coolify/VPS: existing infra or low incremental cost
  - PostHog/analytics: low or free starter tier
  - Resend: low/free starter usage
  - Stripe: transaction-based
  - Optional SEO tools: $0–$200 if using free/manual workflows, up to ~$300/month if temporarily using Ahrefs/LowFruits
- Community/creator spend:
  - $0–$1,500 for small sponsorships or affiliate payouts to micro-creators
- Total 3-month budget from research range: **$1,500–$4,000**
- Founder time:
  - 12–18 hours/week build in month 1
  - 8–12 hours/week marketing/content in months 2–3
  - Approx. **120–180 founder hours** over 3 months

#### Expected CAC
Grounded in the research estimate: **$20–$60 CAC**, mostly via content and creator placement rather than scaled paid ads. SEO CAC can trend lower over time; creator-led or newsletter placements may sit at the higher end early on.

### Why this works at zero/low cost

This playbook works because the user already searches for this exact problem and gathers in niche communities where pricing confusion is common. A free calculator and useful pricing content are cheap to produce, genuinely helpful, and naturally tied to the paid upgrade path.

## Target user

Primary persona: **Owner-operator of a small Etsy shop selling physical products with variable production costs**, especially:
- 3D printed products
- laser-cut items
- handmade goods with material + labor complexity
- custom order work

They usually:
- run the shop solo or with one helper
- price with spreadsheets, rough estimates, notes apps, or memory
- understand gross sales but not true listing-level profit
- get tripped up by Etsy fees, shipping, packaging, machine time, labor, and discounting
- need fast answers like:
  - “Am I making money on this listing?”
  - “How should I quote this custom order?”
  - “Which products should I scale or retire?”

Why current options fail them:
- generic calculators don’t save reusable listing logic
- accounting tools are too broad/heavy for pricing decisions
- maker costing tools don’t handle Etsy-specific fees and listing economics well
- product research tools like EverBee don’t answer “what is my actual margin?”

Where they already hang out online:
- r/EtsySellers
- r/Etsy
- r/3Dprinting
- r/lasercutting
- Etsy Facebook groups
- maker Discord servers
- YouTube channels about Etsy side hustles, 3D print farms, Glowforge/laser businesses

## Core features (MVP)

- **Public free listing profit calculator**
  - Inputs: sale price, Etsy fees, shipping charged, shipping cost, materials, labor time, labor rate, packaging, other per-order costs
  - Outputs: gross profit, margin %, net after fees, warning if margin below threshold

- **Saved listings dashboard**
  - Create, edit, duplicate, archive listings
  - View margin by listing in table/grid format

- **Reusable cost templates**
  - Save material profiles, packaging presets, labor rates, machine-time defaults
  - Apply to new listings quickly

- **Etsy fee presets**
  - Simple fee model with editable defaults
  - Support listing fee, transaction fee, payment processing estimate, offsite ad toggle/manual override

- **Custom quote builder**
  - Create a quote from a saved listing or from scratch
  - Add quantity, customization upcharge, rush fee, shipping, discount
  - Export printable quote page/PDF

- **Profit breakdown view**
  - Per-listing breakdown of revenue, fees, materials, labor, packaging, shipping
  - Visual margin bar and profitability status badge

- **Basic reporting**
  - Top profitable listings
  - Lowest margin listings
  - Simple monthly/seasonal tags or filters

- **CSV import/export**
  - Export listings and quotes
  - Import a simple CSV format for bulk setup

- **Email capture + onboarding**
  - Account signup
  - Guided onboarding with 3 sample listings/templates

- **Billing**
  - Free tier with limited calculator access
  - Paid plans for saved listings, templates, reports, exports, quotes

## Tech stack recommendation

Use the **standard web SaaS stack**:

- **Next.js 15 (App Router) + TypeScript**
- **PostgreSQL + Prisma ORM**
- **Tailwind CSS + shadcn/ui**
- **NextAuth v5 with Google OAuth**
- **Stripe** for subscriptions
- **Resend** for transactional emails
- **PostHog** for product analytics
- **Deployed to Coolify using Docker**

Why this fits:
- This is a browser-based SaaS with auth, saved data, and subscriptions.
- The product benefits from SEO, so server-rendered landing/calculator pages in Next.js are ideal.
- No mobile framework should be used; this should be built as a responsive web app.

## Monetization & pricing

**Primary strategy: freemium SaaS subscription**

This is justified by the research’s pricing fit and the recurring nature of the problem. Sellers revisit pricing when costs change, when they add listings, and when they quote custom work. Comparable products in adjacent categories already prove sellers pay for tooling.

### Recommended pricing

- **Free**
  - Public calculator
  - Up to 3 saved listings
  - 1 cost template
  - No CSV export
  - No custom quote export
  - Goal: convert serious sellers, not serve as full free software

- **Standard — $19/month**
  - Up to 50 saved listings
  - Unlimited cost templates
  - Full profit breakdown dashboard
  - Quote builder
  - CSV export
  - Best for solo Etsy shops

- **Pro — $39/month**
  - Unlimited listings
  - Seasonal analysis/tags
  - Advanced quote templates
  - Duplicate/store multiple fee presets
  - Priority support / feature request lane
  - Best for active shops with larger catalogs or custom order volume

Annual option:
- $190/year Standard
- $390/year Pro

This pricing follows the research’s value case: preventing even a few underpriced orders per month can justify $19–39.

## Keyword / SEO strategy

The research did not provide verified search volumes, so volumes must be validated before committing to content production targets. Strategy should prioritize high-intent long-tail pages rather than broad category terms.

### Primary SEO keywords
- etsy listing margin calculator
- etsy profit tracker for handmade sellers
- 3d print seller cost calculator
- laser cut etsy pricing tool
- etsy 3d print profit calculator
- Etsy fee calculator for handmade sellers

### Long-tail targets
- how to price custom 3d prints on Etsy
- how to price laser cut products on Etsy
- custom order quote template Etsy
- Etsy profit calculator for handmade products
- how to calculate Etsy fees and profit
- best app for Etsy sellers pricing
- apps for Etsy sellers profit tracking

### Suggested meta title format
`{Primary Keyword} | Calculate True Etsy Listing Profit`

### Suggested meta description format
`Free {keyword} for Etsy makers. Calculate fees, materials, labor, packaging, shipping, and true margin for 3D print, laser-cut, and handmade listings.`

### Site structure

- Homepage
- Calculator pages
- Use-case pages by seller type
- Educational articles
- Competitor comparison pages
- Template pages
- Authenticated app dashboard

### Pages to prioritize first
1. Etsy listing margin calculator
2. Etsy fee calculator for handmade sellers
3. 3D print seller cost calculator
4. Laser cut Etsy pricing tool
5. How to price custom 3D prints on Etsy

## Revenue model

This model should stay conservative and tie directly to the SEO + community warm-start strategy.

### Funnel assumptions
- Free calculator visitors convert to email/signup better than homepage visitors
- A subset of signups saves listings
- Users who save 2–3 listings and build one quote are the highest-probability paid conversions

Illustrative funnel:
- Visitor → calculator use
- Calculator use → signup
- Signup → saved listing created
- Saved listing created → paid conversion

### Month 1
- Traffic: 200–400 visitors, mostly community/beta/direct
- Signups: 20–40
- Paying users: 5–10
- ARPU: ~$19–$23 blended
- Revenue: **$95–$230 MRR**

### Month 3
- Traffic: 1,000–2,000 monthly visitors from early SEO + community
- Signups: 100–180 cumulative
- Paying users: 20–40
- Blended ARPU: ~$22–$25
- Revenue: **$440–$1,000 MRR**

### Month 6
- Traffic: 3,000–6,000 monthly visitors if long-tail pages begin ranking
- Paying users: 50–90
- Blended ARPU: ~$23–$26
- Revenue: **$1,150–$2,340 MRR**

### Month 12
- Paying users: 100–300
- Blended ARPU: ~$24–$28
- Revenue: **$2,400–$8,400 MRR**

This aligns with the research:
- conservative case around **$2,000/month**
- stronger case if distribution works and 100+ users are converted
- upper range of **$12,000/month** would require the 300–400 subscriber scenario from the research, which is possible but should not be treated as the base case

## Competitor landscape

### Craftybase
- URL: https://craftybase.com
- Pricing model: paid SaaS for handmade seller operations (exact prices not provided in research, so do not state them)
- Key features: inventory, bookkeeping, broader operational workflows for handmade sellers
- Specific gaps/weaknesses:
  - broader/heavier positioning
  - can feel too complex for sellers who mainly want listing-level margin clarity
  - less focused on fast custom quoting
  - less tailored to 3D print/laser-cut per-SKU production economics
- Our differentiation:
  - faster listing profit workflow
  - Etsy-specific fee and margin framing
  - machine-time and custom quote support
  - lighter UI centered on pricing decisions

### EverBee
- URL: https://www.everbee.io
- Pricing model: paid Etsy seller tool (exact prices not provided in research)
- Key features: product research and analytics for Etsy sellers
- Specific gaps/weaknesses:
  - focuses on research, not true profitability
  - does not own the production cost + pricing workflow
- Our differentiation:
  - answers “what should I charge and what do I keep?”
  - not a trend tool; a profit decision tool
  - stronger fit for sellers with physical production complexity

### FabLog
- URL: https://www.fablog.io
- Pricing model: maker-oriented costing/profit tool (exact prices not provided in research)
- Key features: production costing, maker profit tracking
- Specific gaps/weaknesses:
  - general maker/manufacturing orientation
  - likely misses Etsy fee structures and listing-based store economics
  - lacks Etsy-specific quoting and storefront decision support
- Our differentiation:
  - Etsy-native fee model
  - listing-level views instead of generic jobs/projects
  - custom quote/invoice workflow for Etsy sellers

## Timeline

### Week 1 — Core data model + calculator
- Set up Next.js app, Prisma, Postgres, auth scaffolding
- Build public calculator page
- Implement cost/margin calculation engine
- Create homepage and primary CTA flow
- Define pricing tiers and feature gating rules

### Week 2 — Authenticated MVP dashboard
- Build user auth with Google
- Build saved listings CRUD
- Build reusable templates CRUD
- Build dashboard table with profit metrics
- Add Etsy fee preset model and editable defaults

### Week 3 — Quotes, billing, polish
- Build quote builder
- Build printable/exportable quote view
- Add Stripe subscriptions and gated features
- Add CSV export/import
- Add onboarding flow and sample data
- Add error states, validation, empty states

### Week 4 — Final build work + launch prep
- Add basic reports and seasonal tags/filters
- Add transactional emails
- Add analytics instrumentation
- Improve SEO metadata and page performance
- QA billing, auth, imports, exports, and responsive layouts
- Freeze MVP scope

### Week 5 — Launch
- Deploy production app
- Run launch plan from distribution section
- Patch onboarding friction quickly
- Collect first testimonials and support issues

## Score breakdown

- **recurringRevenue: 8/10** — Pricing is recurring because costs, listings, and quoting needs change continuously for sellers.
- **buildComplexity: 7/10** — Requires calculation logic, CRUD, auth, subscriptions, export, and some reporting, but remains well within solo SaaS scope.
- **marketValidation: 7/10** — Validated by real adjacent tools like Craftybase, EverBee, and FabLog, plus direct keyword intent from research.
- **competition: 6/10** — There are real competitors, but they are adjacent rather than perfectly focused on this specific Etsy profitability wedge.
- **automationPotential: 8/10** — High reuse through templates, presets, recurring calculations, and future imports/reporting.
- **platformFit: 9/10** — Strong fit for a web SaaS because users need saved data, dashboards, quotes, and SEO-acquired calculator pages.
- **distributionFeasibility: 8/10** — Strongest point after platform fit: real long-tail SEO wedge plus identifiable Etsy/maker communities and creator ecosystems.

## Build instructions for the coding agent

1. Initialize project:  
   `npx create-next-app@latest listing-profit-dashboard-etsy-makers --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`

2. Install dependencies:  
   `npm install prisma @prisma/client next-auth @auth/prisma-adapter stripe resend zod react-hook-form @hookform/resolvers date-fns papaparse posthog-js`

3. Install shadcn/ui:  
   `npx shadcn@latest init`  
   Add components: `button`, `card`, `input`, `label`, `table`, `dialog`, `dropdown-menu`, `form`, `select`, `tabs`, `badge`, `sheet`, `alert-dialog`, `toast`

4. Set up Prisma:  
   `npx prisma init`  
   Configure PostgreSQL connection in `.env`

5. Define Prisma schema in `prisma/schema.prisma` with these models:
   - `User`
   - `Account`
   - `Session`
   - `VerificationToken`
   - `Subscription`
   - `Listing`
   - `CostTemplate`
   - `FeePreset`
   - `Quote`
   - `QuoteLineItem`
   - `LeadCapture`
   - `UsageEvent`

6. Use these data shapes:
   - `Listing`: `id`, `userId`, `name`, `sku`, `salePrice`, `shippingCharged`, `shippingCost`, `materialsCost`, `laborMinutes`, `laborRate`, `packagingCost`, `otherCost`, `feePresetId`, `tags`, `isArchived`, `createdAt`, `updatedAt`
   - `CostTemplate`: `id`, `userId`, `name`, `defaultLaborRate`, `defaultPackagingCost`, `defaultOtherCost`, `notes`
   - `FeePreset`: `id`, `userId`, `name`, `listingFee`, `transactionFeePercent`, `paymentFeePercent`, `paymentFeeFixed`, `offsiteAdsPercent`, `includeOffsiteAds`
   - `Quote`: `id`, `userId`, `listingId?`, `customerName`, `title`, `quantity`, `customizationFee`, `rushFee`, `discountAmount`, `shippingCost`, `notes`, `status`
   - `Subscription`: `id`, `userId`, `stripeCustomerId`, `stripeSubscriptionId`, `stripePriceId`, `status`, `currentPeriodEnd`

7. Run Prisma generate and migration:  
   `npx prisma generate`  
   `npx prisma migrate dev --name init`

8. Create database client in `src/lib/db.ts` and shared auth config in `src/lib/auth.ts`.

9. Configure NextAuth v5 with Google OAuth and Prisma adapter.  
   Files:
   - `src/auth.ts`
   - `src/app/api/auth/[...nextauth]/route.ts`
   - `src/middleware.ts` for protecting app routes

10. Build the calculation engine in `src/lib/profit.ts` with pure functions:
   - `calculateFees(input)`
   - `calculateLaborCost(input)`
   - `calculateProfit(input)`
   - `calculateMargin(input)`
   - `buildQuoteTotals(input)`
   Return typed objects so the same logic is used in public calculator, dashboard, and quotes.

11. Create marketing/public pages:
   - `src/app/page.tsx`
   - `src/app/etsy-listing-margin-calculator/page.tsx`
   - `src/app/etsy-fee-calculator-handmade-sellers/page.tsx`
   - `src/app/3d-print-seller-cost-calculator/page.tsx`
   - `src/app/laser-cut-etsy-pricing-tool/page.tsx`
   - `src/app/how-to-price-custom-3d-prints-on-etsy/page.tsx`
   Each should be server-rendered and include strong CTA to sign up.

12. Build public calculator component:
   - `src/components/calculator/public-profit-calculator.tsx`
   - Use `react-hook-form` + `zod`
   - Include fields for all cost inputs
   - Show live results panel with fees, total cost, profit, and margin
   - Gate “save this listing” behind auth

13. Build app shell and dashboard routes:
   - `src/app/app/layout.tsx`
   - `src/app/app/dashboard/page.tsx`
   - `src/app/app/listings/page.tsx`
   - `src/app/app/listings/new/page.tsx`
   - `src/app/app/listings/[id]/page.tsx`
   - `src/app/app/templates/page.tsx`
   Use server components for data fetch, client components only where interactivity is needed.

14. Implement listings CRUD:
   - Components:
     - `src/components/listings/listing-form.tsx`
     - `src/components/listings/listings-table.tsx`
     - `src/components/listings/profit-breakdown-card.tsx`
   - API/actions:
     - `src/app/app/listings/actions.ts`
   - Support create, update, duplicate, archive

15. Implement cost templates and fee presets:
   - `src/components/templates/template-form.tsx`
   - `src/components/fees/fee-preset-form.tsx`
   - Allow autofill when creating listings
   - Seed one default Etsy preset for new users

16. Implement quote builder:
   - Routes:
     - `src/app/app/quotes/page.tsx`
     - `src/app/app/quotes/new/page.tsx`
     - `src/app/app/quotes/[id]/page.tsx`
     - `src/app/q/[id]/page.tsx` for printable share/export view
   - Components:
     - `src/components/quotes/quote-form.tsx`
     - `src/components/quotes/quote-preview.tsx`
   - Add browser print stylesheet and PDF-friendly layout

17. Implement CSV import/export:
   - `src/components/listings/import-csv-dialog.tsx`
   - `src/app/app/import/page.tsx`
   - Export endpoint: `src/app/api/export/listings/route.ts`
   - Parse CSV with PapaParse and validate rows with zod

18. Implement Stripe billing:
   - Create products/prices for Free, Standard, Pro
   - Files:
     - `src/lib/stripe.ts`
     - `src/app/api/webhooks/stripe/route.ts`
     - `src/app/api/stripe/checkout/route.ts`
     - `src/app/app/billing/page.tsx`
   - Gate by plan:
     - Free: max 3 listings, 1 template
     - Standard: up to 50 listings
     - Pro: unlimited
   - Add helper `src/lib/plan.ts` for entitlements

19. Add Resend transactional emails:
   - welcome email
   - trial/upgrade confirmation
   - quote-created notification if needed
   Files:
   - `src/lib/email.ts`
   - `src/emails/welcome-email.tsx`

20. Add analytics and lead capture:
   - PostHog client in `src/components/analytics/posthog-provider.tsx`
   - Track events:
     - calculator_used
     - signup_started
     - listing_created
     - quote_created
     - checkout_started
     - subscription_started
   - Add `LeadCapture` form on SEO pages for non-signup users

21. Add SEO infrastructure:
   - per-page metadata in each route
   - `src/app/sitemap.ts`
   - `src/app/robots.ts`
   - JSON-LD for SoftwareApplication and FAQ where relevant

22. Add sample data seeding script:
   - `prisma/seed.ts`
   - Seed example listings for:
     - 3D printed planter
     - laser-cut sign
     - custom cake topper

23. Add responsive polish and empty states:
   - mobile-friendly table fallback cards
   - dashboard KPI cards
   - onboarding checklist component in `src/components/onboarding/checklist.tsx`

24. Verify all flows:
   - anonymous calculator
   - auth signup
   - save listing
   - edit listing
   - quote creation
   - CSV import/export
   - upgrade/downgrade billing
   - protected route access
   - SEO page rendering

25. Finalize deployment:
   - set `output: "standalone"` in `next.config.ts`
   - create `Dockerfile`
   - ensure env vars documented in `.env.example`
   - run `npm run build`
   - deploy to Coolify and verify Stripe webhook delivery and Google OAuth callback URLs


---

## PRD Completion Contract

This build is not complete until the implementation covers the whole PRD, not just a scaffold or a single happy path.

Before coding:

1. Read the PRD end-to-end.
2. Create `FORGE_PRD_TASKS.md`.
3. Break the PRD into an explicit checklist covering data model, auth, every user-facing page, every API/server action, every core workflow, billing/email/storage integrations or safe fallbacks, marketing/SEO pages, Docker/deploy config, and verification.

During coding:

1. Implement the checklist in dependency order: foundation -> data/auth -> core workflows -> secondary workflows -> marketing/pages -> deployment -> QA.
2. After each major phase, re-read the relevant PRD sections and update `FORGE_PRD_TASKS.md`.
3. Use realistic local/mock/safe-fallback implementations when external credentials are unavailable.
4. Do not leave placeholder pages, TODO-only routes, fake buttons, or unimplemented workflows.

Before finishing:

1. Run `npm run build` and fix all failures.
2. Start the dev server and smoke-test primary routes.
3. Create `FORGE_COMPLETION_AUDIT.md` mapping every major PRD requirement to concrete files/routes/components/actions that implement it.
4. List any truly external credential requirements in `HUMAN_INPUT_NEEDED.md`, but only after implementing guarded code paths and safe fallbacks.
5. Output `FORGE_BUILD_COMPLETE` only after the task ledger and audit exist.

---

## Dockerfile

**Forge writes the Dockerfile automatically after your build completes — do NOT author or modify a Dockerfile yourself.** It will be overwritten with the canonical template (`DOCKERFILE_SIMPLE` for apps without Prisma, `DOCKERFILE_PRISMA` if `package.json` includes `@prisma/client`).

Your responsibility: ensure `next.config.ts` (or `next.config.mjs`) sets `output: "standalone"` so the standalone bundle is produced by `npm run build`. If you skip that, the Dockerfile's `COPY .next/standalone` step fails at deploy time.

---

## File Organization

- Keep source code in `src/` (if applicable)
- Components in `src/components/`
- Utilities in `src/lib/` or `src/utils/`
- API routes in `src/app/api/` (Next.js) or equivalent
- Static assets in `public/`
- Environment variables in `.env.local` (never commit secrets)

---

## Build Verification

Run `npm run build` and ensure zero errors before committing.

- No TypeScript errors
- No ESLint warnings (fix or disable with justification)
- All pages/screens render without crashes
- API routes return expected responses

---

## Git Instructions

When the build is complete and verified:

1. Initialize the repository if not already done: `git init`
2. Stage all files: `git add -A`
3. Commit: `git commit -m "Initial build: listing-profit-dashboard-etsy-makers"`
4. Push to remote when instructed

---

## Required Files Checklist

- [ ] `README.md` - Project description, setup instructions, tech stack
- [ ] `.gitignore` - Appropriate for Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- [ ] `.env.example` - Template for required environment variables (see below)
- [ ] `package.json` (or equivalent) - Dependencies and scripts

---

## Zero-Config Deployment Requirement

**CRITICAL: This app MUST start and run correctly with NO environment variables configured in the deployment platform (Coolify). All external service dependencies must either be absent or have safe fallback defaults baked in.**

Apps that crash on startup due to missing env vars will fail every deployment cycle.

**CRITICAL: Never instantiate third-party API clients at module level.** Next.js evaluates top-level code during `next build` — a `new Resend(...)`, `new Stripe(...)`, or `new OpenAI(...)` call at module scope will crash the build if the API key env var is missing. Always initialize these clients **inside** the handler/function body, guarded by an env var check.

---

## Environment Variables (.env.example)

Only include variables the app actually uses. If the PRD doesn't require a feature, don't add those variables.

```bash
# App URL (optional — remove if not needed)
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# --- Only include below if PRD requires data persistence ---
# Database (SQLite — no external service required)
DATABASE_URL="file:./dev.db"

# --- Only include below if PRD requires user login ---
# NextAuth secret — a default is baked into the Dockerfile; override here for production
AUTH_SECRET="your-secret-here"  # generate with: openssl rand -base64 32

# --- Only include below if PRD explicitly requires payments ---
# STRIPE_SECRET_KEY="sk_live_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Only include below if PRD explicitly requires email ---
# RESEND_API_KEY="re_..."
# EMAIL_FROM="noreply@yourdomain.com"
```

---

## Key Implementation Notes

### Database (only if PRD requires persistence)
- Use **SQLite** via Prisma: `provider = "sqlite"`, `url = env("DATABASE_URL")`
- `DATABASE_URL` defaults to `file:/data/app.db` in the Dockerfile — no Coolify config needed
- For local dev: `DATABASE_URL="file:./dev.db"` in `.env.local`
- Run `npx prisma db push` after schema changes
- **Do NOT use PostgreSQL** — no PostgreSQL server is provisioned in the deployment environment

### Authentication (only if PRD requires user login)
- Use **NextAuth v5** with the **Credentials provider** (email + password)
- **Do NOT use Google OAuth** — no OAuth credentials are configured in deployment
- Hash passwords with **bcrypt** (`bcryptjs` package)
- Session strategy: **`"jwt"`** (not `"database"`) — required for Credentials provider

### Public Routes Contract (CRITICAL when adding auth)

If you add auth, the following routes MUST render to status 200 for an unauthenticated visitor — they are how customers find the product, evaluate it, and sign up:

- `/` — homepage / landing
- `/pricing` — pricing page (if pricing exists)
- `/blog`, `/blog/[slug]` — blog (if any)
- `/about`, `/contact`, `/legal/*` — marketing supporting pages
- `/login`, `/signup` — auth entry points themselves

Implementation rules:
- Place public pages OUTSIDE any `(app)` / `(authenticated)` / `(dashboard)` route group, AND outside any layout that calls `requireUser()` / `auth()` / `getServerSession()`.
- Authenticated routes go INSIDE a route group whose layout gates with `requireUser()` (e.g. `src/app/(app)/dashboard`, `src/app/(app)/settings`, `src/app/(app)/billing` if billing is the user's own subscription page). The verifier knows about Next.js route groups and expects these to redirect to `/login` when visited anonymously.
- If you write a `middleware.ts`, its `matcher` MUST exclude the public routes above. A blanket matcher like `/((?!api|_next).*)` that protects everything is a build failure.
- A user with no session who visits `/` and lands on `/login` is a build failure — Forge's verifier will detect it and reject the build.

If the PRD does NOT require auth, do NOT add NextAuth at all. No middleware, no `requireUser`, no `(app)` group — just public pages.
- `AUTH_SECRET` defaults are baked into the Dockerfile; app works out of the box

### Payments (only if PRD explicitly requires it)
- Use Stripe only when the PRD's core functionality requires payment processing
- If adding Stripe: handle `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted` at `/api/webhooks/stripe`
- Make the app functional without Stripe credentials (show upgrade prompts rather than crashing)

### Email (only if PRD explicitly requires it)
- Use Resend only when the PRD requires transactional email
- **CRITICAL: NEVER instantiate `new Resend(...)` at module level** — always inside the handler function body with a guard:
  ```ts
  // WRONG — causes build failure (next build evaluates module-level code):
  const resend = new Resend(process.env.RESEND_API_KEY);
  export async function POST(req) { ... }

  // CORRECT — lazy init inside handler:
  export async function POST(req) {
    if (!process.env.RESEND_API_KEY) {
      console.log("[email] RESEND_API_KEY not set, skipping email");
      return NextResponse.json({ ok: true });
    }
    const { Resend } = await import("resend");
    const resend = new Resend(process.env.RESEND_API_KEY);
    ...
  }
  ```
- Next.js evaluates module-level code during `next build` page data collection — missing env vars at module scope crash the build
- Same rule applies to Stripe, OpenAI, and ALL third-party SDK clients — always lazy-initialize inside handler functions

### Fonts
- **Do NOT use `next/font/google`** — it fetches font files during `next build`, which can hang or fail in automated/offline build environments.
- Use CSS/system font stacks by default.
- If a custom font is truly required, commit the font file into the repo and use `next/font/local`; never depend on a build-time network font fetch.

### Deployment
- Coolify with Docker — always set `output: "standalone"` in next.config.ts
- Do NOT add `COPY public ./public` in the Dockerfile if the public/ directory is empty or doesn't exist
- The Dockerfile's CMD initializes the SQLite database on first start via `prisma db push`
- App must respond healthy on port 3000 within 60 seconds of container start
- **CRITICAL Dockerfile rule**: In the `deps` stage, always use `RUN npm ci --ignore-scripts`. Without `--ignore-scripts`, the `postinstall` hook (e.g. `prisma generate`) runs before `prisma/schema.prisma` is copied, causing the build to fail. Run `npx prisma generate` explicitly in the `builder` stage after `COPY . .`.
- **Do NOT run `npx prisma db push` in the Dockerfile builder stage** — it is not needed at build time. The CMD already runs `prisma db push` at container startup when a real `/data/app.db` path is available.
- **Use `node:20-slim` (not `node:20-alpine`)** — Debian slim avoids Alpine musl/libssl issues. Add `RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*` to BOTH the builder and runner stages for Prisma's schema engine.
- **Set `binaryTargets` in schema.prisma** — always include `binaryTargets = ["native", "debian-openssl-3.0.x"]` in the Prisma generator block so Prisma generates the correct engine binary for the Debian container environment.
- **Copy full node_modules in the runner stage** — do NOT selectively copy `node_modules/.prisma`, `node_modules/prisma`, `node_modules/@prisma` separately. Instead use `COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules` to copy everything. Prisma v6+ CLI has deep transitive deps (`@prisma/config` → `effect`, `c12`, etc.) that break if not all present.

